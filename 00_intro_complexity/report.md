# Chapter 00 Report — Complexity & Feasibility

## 1. 实验回顾

本章通过三个极其简单的函数，刻意对比了三种不同的复杂度：

- O(n)：线性
- O(n²)：二次
- O(2ⁿ)：指数

实验本身没有任何“智能”成分，
但它回答了一个比算法细节更基础的问题：

> **哪些想法，在 AI 世界中是“能活下来的”？**

---

## 2. 关键观察（不看数值，看趋势）

### 2.1 线性复杂度：可规模化的底线

在 O(n) 的情况下：

- 输入规模变大
- 运行时间近似线性增长

👉 这类计算**不会在规模上“突然崩塌”**。

这让我意识到：

> **在 AI 系统中，O(n) 是“可长期存在”的计算形态。**

例如：
- token 级线性遍历
- 流式推理
- 简单聚合

---

### 2.2 二次复杂度：危险但常见

O(n²) 的变化非常直观：

- n 稍微增大
- 时间明显恶化

👉 这正是 Transformer 中 attention 的现实写照。

它不是立刻不可用，
但**规模一旦扩大，就会变成系统瓶颈**。

这让我建立了一个重要直觉：

> **很多 AI 架构不是“算不对”，而是“算不起”。**

---

### 2.3 指数复杂度：方法论层面的淘汰

O(2ⁿ) 的函数在 n 很小的时候就已经开始卡顿。

这说明：

- 理论上完备
- 逻辑上正确
- 但现实中不可运行

👉 **这是“被现实直接否决”的一类方法。**

在 AI 语境下，这对应：

- 无剪枝搜索
- 穷举推理
- 完备但不可扩展的算法

---

## 3. 数据结构 → AI 的认知映射

| 算法复杂度 | AI 中的含义 |
|-----------|------------|
| O(n) | 可规模化推理 |
| O(n²) | 长上下文 / 全连接瓶颈 |
| O(2ⁿ) | 搜索空间爆炸 |
| 跑不动 | 方法论上不可行 |

通过这个实验可以清楚看到：

> **AI 的第一性约束不是“聪不聪明”，  
而是“算不算得动”。**

---

## 4. 本章的核心认知收获

这一章让我意识到一个非常基础、但常被忽略的事实：

> **复杂度不是优化问题，而是生存问题。**

一个方法如果在复杂度上不成立，
即使它在论文中再优雅、再完美，
也无法进入真实的 AI 系统。

这解释了为什么现代 AI：

- 依赖近似
- 接受不完备
- 偏好启发式而不是穷举

---

## 5. 向后展望

如果说这一章解决的是：

> **“什么样的计算在 AI 中是可能的？”**

那么下一章自然引出：

> **“AI 实际在计算什么？”**

也就是：

👉 **向量表示（Embedding）**

从这一刻开始，
“数据结构”将不再是抽象对象，
而是直接成为 **AI 表示世界的方式**。

---

## 6. 一句话总结（给未来的自己）

> **在 AI 中，  
正确性只是前提，  
可计算性才是门槛。**


---
# 输出结果
```
=== Chapter 01: Complexity & Feasibility (AI View) ===

[ O(n) — Scalable ]
n = 10000    time = 0.000694s
n = 50000    time = 0.003551s
n = 100000   time = 0.005447s

[ O(n^2) — Expensive ]
n = 200      time = 0.002424s
n = 400      time = 0.010382s
n = 600      time = 0.023135s

[ O(2^n) — Infeasible ]
n = 10       time = 0.000021s
n = 20       time = 0.002107s
n = 30       time = 0.155255s
```

# 理解
✅ 实现三个函数

linear → O(n)

quadratic → O(n²)

exponential → O(2ⁿ)

✅ 输入规模逐步增大

O(n)：10k → 50k → 100k

O(n²)：200 → 400 → 600

O(2ⁿ)：10 → 20 → 30

✅ 记录运行时间

measure() 统一计时

控制变量清晰


